GEMINI_API_KEY=your-api-key-here
GEMINI_MODEL=gemini-2.5-flash
PORT=3000
DATABASE_PATH=./data/awafiler.db

# Set to "true" to enable the ERP sync feature (requires bun run dev:server + VPN)
VITE_ERP_ENABLED=false
